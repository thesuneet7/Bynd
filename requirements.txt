# Core
pydantic>=2.6
python-dotenv>=1.0
httpx>=0.27
tenacity>=8.2

# LLMs
anthropic>=0.39          # Claude (writer / structural)
openai>=1.40             # used as OpenAI-compatible client for xAI Grok (verifier)

# Tools / services
duckduckgo-search>=6.0   # legacy name; runtime uses `ddgs` package
ddgs>=9.0                # free web search (no API key; default discovery)
tavily-python>=0.5       # optional paid search fallback (LLM-optimized snippets)
firecrawl-py>=1.6        # scraping / clean extraction (comfortable budget)
llama-cloud-services>=0.6 # LlamaParse (annual-report table extraction)

# Local retrieval (no external embedding key needed)
fastembed>=0.3           # ONNX local embeddings (BAAI/bge-small-en-v1.5)
numpy>=1.26

# Parsing / rendering
pypdf>=5.0               # cheap local text pass to locate relevant pages before LlamaParse
openpyxl>=3.1            # local Excel workbook extraction for archived financial docs
selectolax>=0.3          # fast HTML -> text fallback
jinja2>=3.1              # HTML rendering
