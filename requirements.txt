# Core
pydantic>=2.6
python-dotenv>=1.0
httpx>=0.27
tenacity>=8.2

# LLM (products/customers extraction from filings)
anthropic>=0.39

# Web scraping (unlisted company website crawl)
firecrawl-py>=4.0
playwright>=1.40

# Document parsing
llama-cloud-services>=0.6
pypdf>=5.0
selectolax>=0.3
ddgs>=9.0               # tofler.in company resolution only

# Optional screener login fallback
# playwright>=1.40  # install via: pip install -e ".[browser]"
